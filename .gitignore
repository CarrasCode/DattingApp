# ===========================
# PYTHON & DJANGO (Backend)
# ===========================
__pycache__/
*.py[cod]
*$py.class
*.log

# Entornos Virtuales (UV)
.venv/
# Nota: uv.lock se sube, igual que pnpm-lock.yaml

# Django Base
local_settings.py
db.sqlite3
db.sqlite3-journal

# Django Media & Static
media/
staticfiles/

# Calidad de Código (Ruff, Pytest, MyPy)
.ruff_cache/
.pytest_cache/
.mypy_cache/
htmlcov/
.coverage

# ===========================
# NODE & ANGULAR (Frontend)
# ===========================
# Dependencias (Aplica a raíz y subcarpetas)
node_modules/

# Logs de npm/yarn/pnpm
npm-debug.log
yarn-error.log
pnpm-debug.log

# Angular - Build y Cache (Generalizado)
dist/
tmp/
out-tsc/
bazel-out/
.angular/
.sass-cache/
connect.lock
coverage/

# Testing y Screenshots
testem.log
__screenshots__/

# ===========================
# SISTEMA & IDEs
# ===========================
# Variables de entorno (CRÍTICO: No subir nunca)
.env
.env.local
.env.*
!.env.example

# Mac & Windows
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# IDEs
.idea/
*.swp
*.swo
# NOTA: No ignoramos .vscode/ porque queremos compartir launch.json y extensions.json
# Si tienes ajustes personales en vscode que no quieres subir, ignóralos explícitamente así:
# .vscode/settings.json (Descomenta esto solo si tus settings tienen rutas absolutas de tu PC)

# ===========================
# AI & OTROS
# ===========================
.agent/
.gemini
AI_BEST_PRACTICES.md
# ^ Opcional: Si quieres que cada dev tenga sus propias reglas de IA, ignóralo.
# Si quieres que todo el equipo use las mismas reglas (recomendado), ponle un # delante para no ignorarlo.
